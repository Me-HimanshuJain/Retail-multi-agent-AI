[pytest]
minversion = 8.0
testpaths = tests
python_files = test_*.py
addopts = -v --strict-markers
markers =
    unit: unit tests
    integration: integration tests
    e2e: end-to-end tests
    smoke: smoke tests
